"""Local helpers used by websearch_harness providers and middleware.

Nothing in this package may import from deerflow.* or any other framework
outside of stdlib + langchain + pydantic + the provider-specific pip pkgs.
"""
